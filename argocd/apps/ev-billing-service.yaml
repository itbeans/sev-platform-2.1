# Phase 3 — automated sync (financial accuracy; 60-day dual-write to MongoDB + PostgreSQL)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ev-billing-service
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: ev-platform

  source:
    repoURL: https://github.com/itbeans/ev-server
    targetRevision: main
    path: scala-services/helm/charts/ev-billing-service
    helm:
      valueFiles:
        - ../../../argocd/values/staging.yaml
        - ../../../argocd/values/image-tags.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: ev-platform

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
