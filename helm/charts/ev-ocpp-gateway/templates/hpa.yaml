{{/*
For ev-ocpp-gateway, prefer KEDA scaling on WebSocket connection count.
Fall back to CPU/memory HPA when KEDA is not available in the cluster.
*/}}
{{- if not .Values.keda.enabled }}
{{- include "ev-common.hpa" . }}
{{- end }}
